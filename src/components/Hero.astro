---

---

<div class="hero">
    <div class="inner">
        <header>
            <h1>A.J. Latocha</h1>
            <p>
                Driven by curiosity, while not confined by tech choices,<br /> I embrace
                the endless journey of software development.
            </p>
        </header>
    </div>
</div>

<style>
    .hero {
        position: relative;
        width: 100%;
        height: auto; /* Override global .hero height: 100vh */
        flex: 1;
        flex-shrink: 0;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        color: #fff;
        /* overflow: hidden; Removed to allow scrolling */
    }


    .inner {
        padding: 2em;
        position: relative;
        z-index: 2;
        /* No transform needed for desktop if layout is correct, or keep tiny adjustment if preferred. 
           But for mobile we want pure flex centering. */
        transition: transform 0.2s ease;
    }

    /* ... typography ... */
    h1 {
        font-size: 3em;
        margin: 0 0 0.5em 0;
        color: #fff;
    }

    p {
        color: rgba(255, 255, 255, 0.75);
        font-size: 1.25em;
        line-height: 1.75;
    }

    @media (max-width: 768px) {
        .hero {
            /* Mobile: Add padding top to account for fixed Nav overlapping the content */
            padding-top: 80px !important;
            display: flex;
            flex-direction: column;
            width: 100%;
        }

        .inner {
            margin: auto;
            padding: 1rem;
        }

        h1 {
            font-size: 1.75em;
        }

        p {
            font-size: 0.9em;
        }

        .inner {
            transform: none; /* Remove manual offset */
        }
    }
</style>
